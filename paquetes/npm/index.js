/* We're learning about NPM the package manager, we can get any package.
Every package has its documentation.
  -Repository
  -Documentation
  -Dependencies

So, we can learn how to use every package and become us more productive.
*/

const isOdd = require("is-odd");

console.log(isOdd(3));
