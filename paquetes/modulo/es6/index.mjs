/* We're using the ES6 syntax to import this module.
  - Nowadays we can use this with any poblem.
  - If this version would be still experimental
    we got to use "node --experimental-modules filename"
*/

import modulo from "./modulo.mjs";

console.log(modulo.prop1);
modulo.saludar();
